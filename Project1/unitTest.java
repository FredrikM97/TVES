import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

class unitTest {

	@Test
	
	/**
    Move car 1 meter forward, returns current position and situation of detected parking spaces
    @throws IndexOutOfBoundsException	if position < 500 ({@code position < 500})
    Pre-condition:
    	* Car position < 500
    	* 
    Post-condition:
    Test-cases:
    */
	void MoveForward(){
        
    }
	
	/**
    Query sensors and filter noise based on 5 input signals, Return the distance of nearest object
    @param 	sensors 					Array of sensors
    @throws	IndexOutOfBoundsException	if length of sensor array is smaller than 5 ({@code len(sensors) < 5})
    											
    @return			 
    Pre-condition:
     	* Query len(sensors) >= 5
    Post-condition:
    Test-cases:
    */
	void isEmpty(){
        
    }
	
	/**
    Move car 1 meter backwards, returns current position and situation of detected parking spaces
    Pre-condition:
    * Car position > 0
    Post-condition:
    Test-cases:
    */
    void MoveBackward(){
        
    }
    
    /**
    Description
    Pre-condition:
    Post-condition:
    Test-cases:
    */
    void Park(){
        
    }
    
    /**
    Description
    Pre-condition:
    Post-condition:
    Test-cases:
    */
    void UnPark(){
        
    }
    
    /**
    Description
    Pre-condition:
    Post-condition:
    Test-cases:
    */
    void WhereIs(){
        
    }
}
